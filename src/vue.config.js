/*var webpack = require('webpack')
	
module.exports = {
  configureWebpack: {
    plugins: [
      new webpack.DefinePlugin({
        'process.env': {
          'SAS_API_URL': JSON.stringify(process.env.SAS_TOKEN_API)
        }
      })
    ]
  }
}*/